name: Update modules in branch

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */2 * * *' # Every 2 hours

jobs:
  run:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
      # TODO
